/*Programa de prueba*/
#include <stdio.h>

signed sumar(int a,int b);
float restar(short,char);

long main (void)
{	
	int x, y = 2;
	multiplicar(x,3);
	return EXIT_SUCCES;
}

float suma(int a)
{
	return (signed) a + 8?4:6;
}

double resta(int b, int c)
{
	char numero = 43;
	b = 2 + 5 - 1 * 9 / 3;
	c = b += 3;
	
	return NULL;
}

