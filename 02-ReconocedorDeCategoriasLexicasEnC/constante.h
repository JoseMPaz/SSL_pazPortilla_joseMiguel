#ifndef _CONSTANTE_H_
#define _CONSTANTE_H_

#include <stdio.h>
#include <stdlib.h>

typedef int * constante_t;

void agregar_constante (constante_t * constante, int valor);
void imprimir_constante (void * dato);


#endif
