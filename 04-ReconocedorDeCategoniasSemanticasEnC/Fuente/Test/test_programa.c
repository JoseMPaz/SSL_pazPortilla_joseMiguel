/*Programa de prueba*/
#include <stdio.h>

signed sumar(int a);
double dividir();
int resto(void);
int restar(char , unsigned );
char multiplicar(signed e, short , long g);

long main (void)
{
	int var1 = 7;
	int var2 = 1+1;
	float var;
	//char var, mu;
	//double pi;
	char mu, epsilon=7,beta;
	char var16 = var1 && var2;
	//getchar(var,'4',7,2.4,"Hola");
	//sumar (2);
	//sumar(2,4);
	//sumar ('c');
}

signed integrar(int m)
{
	a = 3;
}

unsigned convolucionar(int j, double k, char l)
{
	double anios = mu + epsilon;
	char meses = 3 + 9;
	//char segundos = anios + 2.5;
	d = 3 * (8 / 2);
	return e;
}

double derivar(int h, float i)
{
	char variable = 43;

}
