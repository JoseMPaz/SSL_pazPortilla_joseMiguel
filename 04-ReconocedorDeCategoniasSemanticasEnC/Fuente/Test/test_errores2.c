int sumar(int, b = 2); //Error sintactico en la declaracion de parametros

int entero_2 = ; //Error sintactico en declaracion
int j = 2 + % 3; //Error sintactico en declaracion
char k = int; //error sintactico en declaracion
entero_2 = 2 * - ; //Error sintactico en expresion
int a = entero_2, b=entero_3, c=; //Error sintactico 
char h = + ; //Error sintactico 
h = 'd' & ; 

++; //Error sintactico en expresion unaria

int main() {
    if (c >= 0 && ) return 2;
}

int sumar(int a, int b){
    if(){ //expresion sintatico en sentencia de seleccion
        return a+; //Error sintactico en expresion aditiva
    }
}

@@@ //error lexico

float restar(float a, float b){
    if (a>){ //error sintactico en exprecion relacional
        return a*; //Error sintactico en expresion multiplicativa
    }
}

